<Window x:Class="BryanYOLO.Dialogs.SaveProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="另存为项目" 
        Height="700" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" 
                   Text="另存为新项目" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <!-- 项目信息 -->
        <GroupBox Grid.Row="1" Header="项目信息" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                </Grid.ColumnDefinitions>

                <!-- 项目名称 -->
                <TextBlock Grid.Row="0" Grid.Column="0" 
                           Text="项目名称:" 
                           VerticalAlignment="Center" 
                           Margin="0,5"/>
                <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                         x:Name="ProjectNameTextBox" 
                         Margin="0,5"/>

                <!-- 输出路径 -->
                <TextBlock Grid.Row="1" Grid.Column="0" 
                           Text="输出路径:" 
                           VerticalAlignment="Center" 
                           Margin="0,5"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                         x:Name="OutputPathTextBox" 
                         IsReadOnly="True"
                         Margin="0,5,5,5"/>
                <Button Grid.Row="1" Grid.Column="2" 
                        Content="浏览..." 
                        Click="OnBrowseClick"
                        Margin="0,5"/>
            </Grid>
        </GroupBox>

        <!-- 图片范围选择 -->
        <GroupBox Grid.Row="2" Header="选择图片范围" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 总数显示 -->
                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="5"
                           x:Name="TotalImagesText" 
                           FontWeight="Bold" 
                           Margin="0,0,0,10"/>

                <!-- 当前索引 -->
                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5"
                           x:Name="CurrentIndexText" 
                           Foreground="Blue"
                           Margin="0,0,0,10"/>

                <!-- 范围选择 -->
                <TextBlock Grid.Row="2" Grid.Column="0" 
                           Text="从索引:" 
                           VerticalAlignment="Center"/>
                <TextBox Grid.Row="2" Grid.Column="1" 
                         x:Name="FromIndexTextBox" 
                         Text="1"
                         TextChanged="OnRangeChanged"/>

                <TextBlock Grid.Row="2" Grid.Column="2" 
                           Text="到:" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"/>
                <TextBox Grid.Row="2" Grid.Column="3" 
                         x:Name="ToIndexTextBox" 
                         Text="1"
                         TextChanged="OnRangeChanged"/>

                <!-- 快速选择按钮 -->
                <StackPanel Grid.Row="2" Grid.Column="4" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button Content="全选" 
                            Click="OnSelectAllClick" 
                            Margin="5,0" 
                            Padding="10,2"/>
                    <Button Content="仅已标记" 
                            Click="OnSelectAnnotatedClick" 
                            Margin="5,0" 
                            Padding="10,2"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- 统计信息 -->
        <GroupBox Grid.Row="3" Header="选中范围统计" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" 
                           x:Name="SelectedCountText" 
                           FontWeight="Bold" 
                           Margin="0,2"/>
                <TextBlock Grid.Row="0" Grid.Column="1" 
                           x:Name="AnnotatedCountText" 
                           Foreground="Green" 
                           Margin="0,2"/>
                <TextBlock Grid.Row="1" Grid.Column="0" 
                           x:Name="EmptyCountText" 
                           Foreground="Orange" 
                           Margin="0,2"/>
                <TextBlock Grid.Row="1" Grid.Column="1" 
                           x:Name="NotAnnotatedCountText" 
                           Foreground="Red" 
                           Margin="0,2"/>
            </Grid>
        </GroupBox>

        <!-- 选项 -->
        <GroupBox Grid.Row="4" Header="保存选项" Margin="0,0,0,10">
            <StackPanel Margin="5">
                <CheckBox x:Name="IncludeEmptyCheckBox" 
                          Content="包含空标记图片" 
                          IsChecked="True" 
                          Margin="0,5"/>
                <CheckBox x:Name="IncludeNotAnnotatedCheckBox" 
                          Content="包含未标记图片" 
                          IsChecked="False" 
                          Margin="0,5"/>
                <CheckBox x:Name="CopyImagesCheckBox" 
                          Content="复制图片文件到新项目（推荐）" 
                          IsChecked="True" 
                          Margin="0,5"/>
                <CheckBox x:Name="GenerateTrainValCheckBox" 
                          Content="自动分割训练集和验证集 (80%/20%)" 
                          IsChecked="True" 
                          Margin="0,5"/>
            </StackPanel>
        </GroupBox>

        <!-- 预览 -->
        <GroupBox Grid.Row="5" Header="预览（前10张）" Margin="0,0,0,10">
            <ListBox x:Name="PreviewListBox" 
                     MaxHeight="150"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontSize="11"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <!-- 说明 -->
        <TextBlock Grid.Row="6" 
                   TextWrapping="Wrap" 
                   Foreground="Gray" 
                   FontSize="11" 
                   Margin="0,0,0,10">
            <TextBlock.Text>
                说明：
                • 可以选择保存部分图片到新项目
                • 建议只保存已经校准过的图片
                • 新项目将包含图片、标注文件和YAML配置
                • 可用于训练模型后继续推理校准剩余图片
            </TextBlock.Text>
        </TextBlock>

        <!-- 按钮 -->
        <StackPanel Grid.Row="7" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="保存" 
                    Width="100" 
                    Click="OnSaveClick" 
                    IsDefault="True" 
                    Margin="0,0,10,0"/>
            <Button Content="取消" 
                    Width="100" 
                    Click="OnCancelClick" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>